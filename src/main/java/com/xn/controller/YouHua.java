package com.xn.controller;

import org.springframework.util.CollectionUtils;

import java.util.ArrayList;
import java.util.List;

public class YouHua {
    public static void main(String[] args) {
//        if (!CollectionUtils.isEmpty(lastInterruptList) && session.getPlayInterrupt() && playStatus) {
//            if (lastInterruptList.contains(responseIntent)) {
//                response.setPlayWait(false);
//            } else {
//                logger.debug("tibot_response: uniqueId {}, response {}", session.getUniqueId(), "可以打断但不在InterruptList中，不做任何处理");
//                return null;
//            }
//        } else if (CollectionUtils.isEmpty(lastInterruptList) && session.getPlayInterrupt() && playStatus) {
//            response.setPlayWait(false);
//        } else {
//            response.setPlayWait(playWait);
//        }


    }
}
